const postcss = require('postcss'); const fs = require('fs'); const css = fs.readFileSync('minified.css', 'utf8'); try { postcss.parse(css); console.log('Parsed successfully'); } catch (e) { console.error('Error at line', e.line, 'column', e.column); console.error('Reason:', e.reason); console.error(css.substring(Math.max(0, e.column - 30), Math.min(css.length, e.column + 30))); }
